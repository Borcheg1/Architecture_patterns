[project]
name = "patterns"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.1",
    "pytest>=8.4.1",
    "pytest-factoryboy>=2.8.1",
]
